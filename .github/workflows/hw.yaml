name: Hw wf

on:
  push: 
    branches: [ main ]

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: hello world
        run: sudo service mysql start

      - name: create table
        run: sudo mysql -u root -p actionsJava < database.sql
        # run: |
        #   mysql -u root -p
        #   GRANT ALL PRIVILEGES ON *.* TO 'root'@'localhost' IDENTIFIED BY 'password';
        shell: bash
